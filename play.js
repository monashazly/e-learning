let req = {name:"t",age:25,active:true}
const {name,age} = req

const name = req.name
const age = req.age

console.log({name,age})

const addAdmin = (req,res)=>{
    if(req.body.secret == '4163584653'){

    }else{

    }
}